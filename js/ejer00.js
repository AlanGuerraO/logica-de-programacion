/* EJERCICIO:
 * - Crea un comentario en el código y coloca la URL del sitio web oficial del
 *   lenguaje de programación que has seleccionado.
 * - Representa las diferentes sintaxis que existen de crear comentarios
 *   en el lenguaje (en una línea, varias...).
 * - Crea una variable (y una constante si el lenguaje lo soporta).
 * - Crea variables representando todos los tipos de datos primitivos
 *   del lenguaje (cadenas de texto, enteros, booleanos...).
 * - Imprime por terminal el texto: "¡Hola, [y el nombre de tu lenguaje]!" */

// Sitio oficial de JavaScript: https://developer.mozilla.org/es/docs/Web/JavaScript

// Forma de crear un comentario de una sola linea

/* Forma de crear
comentarios de
varias lineas */

// Declaracion de variables
var declaraVar = "Esta es una variable declarada con var";
let declaralet = "Esta es una variable declarada con let"; // la mas recomendada

// Declaracion de los diferentes tipos de datos
let string = "JavaScript";
let numEntero = 10;
let Booleano = true;
let array = [1,2,3,4];
let objeto = document.querySelector('h1');  

// Imprimeir en consola
console.log(`¡Hola, ${string}!`);